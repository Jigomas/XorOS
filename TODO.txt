// --- Процессор ---

// TODO: ELF-загрузчик
// TODO: расширения F / D / C (A уже сделано)

// --- ОС ---

// TODO: вытесняющий планировщик    — полная 32-регистровая рамка в trap.S + sched_yield() из timer_handler (OSTEP гл. 6.3)
// TODO: kfree()                    — свободный список страниц; kalloc сейчас bump без освобождения
// TODO: per-process page tables    — изоляция процессов; поле satp в process.h; требует kfree
// TODO: PROC_BLOCKED + wake-up     — pipe дропает при переполнении; BLOCKED + будить при освобождении (OSTEP гл. 4.4)
// TODO: U-mode процессы            — запускать задачи через mret в PrivMode::U; инфраструктура уже есть (OSTEP гл. 6.2)
// TODO: fork + wait                — создание дочернего процесса и ожидание завершения (OSTEP гл. 5)
// TODO: exec()                     — загрузка flat binary из памяти в адресное пространство процесса (OSTEP гл. 5.3)
// TODO: sbrk(n)                    — расширение адресного пространства процесса через kalloc + vmem_map
// TODO: COW-форк                   — copy-on-write страницы; ref-count в kalloc, sc.w для fault handler
// TODO: заменить массив процессов  — array-of-slots список без malloc (Jigomas/List)
// TODO: MLFQ                       — приоритеты в PCB, понижение при использовании кванта, boost раз в период (OSTEP гл. 8)
// TODO: лотерейный планировщик     — tickets в PCB, LCG-генератор, выбор победителя в sched_yield (OSTEP гл. 9)
// TODO: демо дедлока               — два процесса берут два spinlock-а в разном порядке; watchdog или детектор
// TODO: CoreMark                   — printf stub + static malloc + instrCount
